# @blazium-engine/toolchain-win32-ia32

Windows x86 binary for [@blazium-engine/toolchain](https://www.npmjs.com/package/@blazium-engine/toolchain).

Install the wrapper, not this package:

```bash
npm install -g @blazium-engine/toolchain
```
