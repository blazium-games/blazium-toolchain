# @blazium-engine/toolchain-win32-x64

Windows x64 binary for [@blazium-engine/toolchain](https://www.npmjs.com/package/@blazium-engine/toolchain).

Install the wrapper, not this package:

```bash
npm install -g @blazium-engine/toolchain
```
